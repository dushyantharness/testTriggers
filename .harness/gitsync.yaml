pipeline:
    name: gitsync
    identifier: gitsync
    projectIdentifier: GitSyncCI1
    orgIdentifier: default
    tags: {}
    stages:
        - stage:
              name: gitsync
              identifier: gitsync
              type: CI
              spec:
                  cloneCodebase: true
                  infrastructure:
                      type: KubernetesDirect
                      spec:
                          connectorRef: account.testK8sConnectorvmIzIJziIq
                          namespace: <+input>
                  execution:
                      steps:
                          - step:
                                type: Run
                                name: run1
                                identifier: run1
                                spec:
                                    connectorRef: account.testDockerConnectorTz237jUOWL
                                    image: maven
                                    command: echo <+codebase.repourl>
                                    privileged: false
              variables: []
    properties:
        ci:
            codebase:
                connectorRef: bitbucket
                repoName: springboot
                build: <+input>
