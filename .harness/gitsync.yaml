pipeline:
    name: gitsync
    identifier: gitsync
    projectIdentifier: GitSyncCI1
    orgIdentifier: default
    tags: {}
    stages:
        - stage:
              name: gitsync
              identifier: gitsync
              type: CI
              spec:
                  cloneCodebase: true
                  infrastructure:
                      type: KubernetesDirect
                      spec:
                          connectorRef: account.testK8sConnectorvmIzIJziIq
                          namespace: <+input>
                  execution:
                      steps:
                          - step:
                                type: Run
                                name: run1
                                identifier: run1
                                spec:
                                    connectorRef: account.testDockerConnectorTz237jUOWL
                                    image: maven
                                    command: "code=\"638648a014d393c1ae2a75615004439bc23a38d5\"\r

                                        commit=<+codebase.commitSha>\r

                                        echo commit\r

                                        if [$commit == $code]\r

                                        then true\r

                                        fi"
                                    privileged: false
              variables: []
    properties:
        ci:
            codebase:
                connectorRef: account.testGitConnectorVFfUKHUp3T
                build: <+input>
